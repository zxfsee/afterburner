{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://afterburner.local/schemas/distributed-shard-lineage-evidence-bundle-reconciliation/v1",
  "title": "Distributed Shard Lineage Evidence Bundle Reconciliation",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "receipt_path",
    "bundle_path",
    "desired",
    "current",
    "reconciliation_status"
  ],
  "properties": {
    "schema_version": { "type": "string", "const": "1" },
    "receipt_path": { "type": "string", "minLength": 1 },
    "bundle_path": { "type": "string", "minLength": 1 },
    "desired": { "type": "object" },
    "current": { "type": "object" },
    "reconciliation_status": {
      "type": "string",
      "enum": ["aligned", "needs-update"]
    }
  }
}
