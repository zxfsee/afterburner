{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://afterburner.local/schemas/gpu-scheduler-heartbeat-reconciliation/v1",
  "title": "GPU Scheduler Heartbeat Reconciliation",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "heartbeat_path",
    "current_heartbeat_path",
    "desired",
    "current",
    "reconciliation_status"
  ],
  "properties": {
    "schema_version": { "type": "string", "const": "1" },
    "heartbeat_path": { "type": "string", "minLength": 1 },
    "current_heartbeat_path": { "type": "string", "minLength": 1 },
    "desired": { "type": "object" },
    "current": { "type": "object" },
    "reconciliation_status": {
      "type": "string",
      "enum": ["aligned", "needs-update"]
    }
  }
}
